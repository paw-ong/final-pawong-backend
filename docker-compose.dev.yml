version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: pawong-dev-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=${LOCAL_MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${LOCAL_MYSQL_DATABASE}
      - MYSQL_USER=${LOCAL_MYSQL_USER}
      - MYSQL_PASSWORD=${LOCAL_MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - local-net

  elasticsearch:
    build:
      context: ./infra/elasticsearch
      dockerfile: Dockerfile
      args:
        ELK_VERSION: ${ELK_VERSION}
    container_name: pawong-dev-elasticsearch
    environment:
      - discovery.type=single-node
      - ES_JAVA_OPTS=${ES_JAVA_OPTS}
      - xpack.security.enabled=false
      - ELASTIC_PASSWORD=${ELASTIC_PASSWORD}
      - ELASTICSEARCH_PLUGINS=analysis-nori
    volumes:
      - ./infra/elasticsearch/config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - ./infra/elasticsearch/data:/usr/share/elasticsearch/data
    ports:
      - "${ELASTIC_PORT}:9200"
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - local-net

  kibana:
    image: kibana:${ELK_VERSION}
    container_name: pawong-dev-kibana
    depends_on:
      - elasticsearch
    ports:
      - "${KIBANA_PORT}:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
    networks:
      - local-net

  nginx:
    image: nginx:latest
    container_name: pawong-dev-nginx
    ports:
      - "80:80"
    volumes:
      - ./infra/nginx/conf/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./infra/nginx/logs:/var/log/nginx
    depends_on:
      - mysql
      - elasticsearch
      - kibana
    networks:
      - local-net

volumes:
  mysql-data:

networks:
  local-net:
    driver: bridge
