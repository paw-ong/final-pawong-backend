api:
  service-key: ${SERVICE_KEY}
data:
  elasticsearch:
    schema-management:
      create-index: false
      create-mappings: false
adoption:
  batch-size: 50
spring:
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: true